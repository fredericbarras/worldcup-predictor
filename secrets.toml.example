# Template for Streamlit secrets.
#
# Local use:  copy to .streamlit/secrets.toml (that path is gitignored).
# Cloud use:  paste the two lines below into share.streamlit.io ->
#             your app -> Settings -> Secrets.

APP_PASSWORD = "choose-a-short-password"
ODDS_API_KEY = "your-the-odds-api-key"
